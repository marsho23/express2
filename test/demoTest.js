const { expect } = require("chai");
const checkPrime = require("../maths.js");

describe("test suite", () => {
  it("should equal 2", () => {
    expect(1 + 1).to.equal(2);
  });
  // it("should not be prime",()=>{
  //   expect(checkPrime(1).to.equal("false"));
  // })
});

// describe("test suite", () => {
//   it("should equal jimmy", () => {
//     expect("").to.equal("jimmy");//   });
// });
